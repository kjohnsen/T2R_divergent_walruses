<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="ClaimColorButton" parent="Base.Widget.AppCompat.Button">
        <item name="android:layout_margin">16dp</item>
    </style>

    <style name="overlayBox">
        <item name="android:background">#55000000</item>
        <item name="android:padding">4dp</item>
        <item name="android:layout_margin">4dp</item>
        <item name="android:textColor">@color/lightGray</item>
    </style>

    <style name="overlaidItem">
        <item name="android:textColor">@color/lightGray</item>
    </style>

    <style name="ttrBackground">
        <item name="android:background">@drawable/ttr_background</item>
    </style>

</resources>
