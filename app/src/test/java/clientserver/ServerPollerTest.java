package clientserver;

import android.content.Intent;

import org.junit.*;
import org.junit.Assert.*;
import org.mockito.Mock;
import static org.mockito.Mockito.*;

import java.lang.invoke.MutableCallSite;
import java.util.concurrent.TimeUnit;

import model.MockServerProxy;

public class ServerPollerTest {
//    @Mock
//    private MockServerProxy mockServerProxy;
//
//    @Before
//    public void setup() {
//        ServerPoller.getInstance().setServerProxy(mockServerProxy);
//    }
//
//    @Test
//    public void getCommands() {
//        ServerPoller.getInstance().startService(new Intent());
//        try {
//            TimeUnit.SECONDS.sleep(2);
//            verify(mockServerProxy, times(1)).getCommands(anyString(), anyString());
//        } catch (InterruptedException e) {
//            e.printStackTrace();
//        }
//    }
}
